# split-background-and-pmf-layers

Split today's monolithic diligence chain into two independently-runnable layers: background-check (cheap, public, no personas) and pmf-deep (the existing pmf-signal, the unique simulation layer). Decouple customer-debrief into its own skill. Relax pmf-signal preflight contract. Foundation for fleet-scale automation.
