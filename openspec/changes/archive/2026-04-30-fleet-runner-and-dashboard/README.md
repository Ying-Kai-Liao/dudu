# fleet-runner-and-dashboard

Add fleet-run skill that orchestrates background-check + optional pmf-signal across many deals in parallel with concurrency caps and per-deal failure isolation, plus a render-dashboard.py script that produces a sortable cross-deal HTML dashboard from per-deal artifacts.
